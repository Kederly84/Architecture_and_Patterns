Добрый день.

Для запуска используется команда uwsgi --http :8080 --wsgi-file run.py,
можно так же запускать через gunicorn с командой gunicorn --bind=127.0.0.1:8080 run:application
Так же изменил адрес работы сервера на 127.0.0.1:8000 субъективно мне так несколько удобнее,
т.к. Django запускается на данном адресе.
Пример страницы брал с сайта приложенного к материалам урока и немного доработал (убрал лишние страницы).